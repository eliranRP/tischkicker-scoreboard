# frozen_string_literal: true

source "https://rubygems.org"

# Used by `bundle exec fastlane {test,beta,release}`.
# Lockfile is committed so CI and a local Mac install the same Fastlane.
gem "fastlane", "~> 2.220"
